"use strict";

/**************************************************
Week 6: Arrays
Melissa Banoen-Garde

Experimenting with arrays.
**************************************************/

// our user, to move with the mouse
let user = {
  x:0,
  y:0,
  size:100
};

// let first food object
let food1 = {
  x: 250,
  y:300,
  size: 50,
  eaten: false // We want to track whether the user has eaten the food
};

let food2 = {
  x: 350,
  y:300,
  size: 50,
  alive: false
};

function setup() {
  createCanvas(windowWidth, windowHeight);
}


function draw() {

}
